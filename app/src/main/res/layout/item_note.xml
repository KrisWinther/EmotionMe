<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/field_light"
    android:padding="16dp"
    android:layout_marginBottom="12dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/tvNoteMood"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@color/black" />

        <!-- Компактный индикатор тональности текста заметки (TextAnalyzer.sentimentScore):
             цветной кружок с эмодзи. Скрывается, если заметка пустая (анализировать нечего). -->
        <TextView
            android:id="@+id/tvNoteSentiment"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_marginEnd="8dp"
            android:gravity="center"
            android:textSize="13sp"
            android:background="@drawable/field_light"
            android:visibility="gone" />

        <TextView
            android:id="@+id/tvNoteDate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textColor="#888888" />

        <ImageView
            android:id="@+id/btnDeleteNote"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginStart="10dp"
            android:padding="4dp"
            android:src="@drawable/outline_delete_24"
            android:contentDescription="Delete entry"
            android:background="?attr/selectableItemBackgroundBorderless"
            app:tint="?attr/themeAccentColor"
            android:alpha="0.6"/>

    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#DDDDDD"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp" />

    <TextView
        android:id="@+id/tvNoteText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="15sp"
        android:textColor="@color/black"
        android:lineSpacingMultiplier="1.3" />

    <!-- Результат офлайн-анализа текста (TextAnalyzer): категория события
         и ключевые слова. Скрывается целиком, если анализировать было нечего
         (пустая заметка или ключевые слова не найдены) -->
    <TextView
        android:id="@+id/tvNoteAnalysis"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:textSize="12sp"
        android:textStyle="italic"
        android:textColor="#888888"
        android:visibility="gone" />

</LinearLayout>